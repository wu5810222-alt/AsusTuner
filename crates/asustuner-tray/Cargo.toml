[package]
name = "asustuner-tray"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AsusTuner 轻量系统托盘（ksni/StatusNotifierItem，右键菜单控制）"

[[bin]]
name = "asustuner-tray"
path = "src/main.rs"

[dependencies]
ksni = { version = "0.3", default-features = false, features = ["blocking", "async-io"] }
png = "0.17"
serde_json = "1"
